import { CuesDirective } from './cues.directive';

describe('CuesDirective', () => {
  it('should create an instance', () => {
    const directive = new CuesDirective();
    expect(directive).toBeTruthy();
  });
});
